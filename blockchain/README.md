# Blockchain Audit Layer

This directory contains the Solidity smart contracts intended for the Polygon testnet to act as an immutable audit layer.

Currently, this is a **starter implementation**. The contract `EnterpriseAudit.sol` is not yet deployed to Polygon.
